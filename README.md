# LedgerScope
LedgerScope is a financial simulation engine modeling economics via two stateless elements: multi-dimensional boundaries (Variables) and mathematical rules (Models).  By chaining models sequentially, it flows data like a rolling ledger to eliminate loops, driving a strategic dashboard of heatmaps, regressions, and risk histograms.

Author & Owner: @hanyuw